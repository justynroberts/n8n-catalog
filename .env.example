# n8n Catalog Environment Configuration

# === Authentication ===
# JWT Secret for authentication (generate a secure random string)
JWT_SECRET=your-secure-random-string-here-change-in-production

# === API Keys ===
# OpenAI API Key (optional, can be set in UI)
OPENAI_API_KEY=

# === Database Configuration ===
# Database path (optional, defaults to /app/data/workflows.db)
DATABASE_PATH=/app/data/workflows.db

# === Server Configuration ===
# Node environment
NODE_ENV=production

# Application port (default: 3000)
PORT=3000

# Server hostname (0.0.0.0 for container, localhost for development)
HOSTNAME=0.0.0.0

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# === Import/Export Configuration ===
# Default import file path for data import script
IMPORT_PATH=/app/data/export.json

# Upload directory for file imports
UPLOAD_DIR=/app/uploads

# === Cache Configuration ===
# Memory cache TTL in milliseconds (5 minutes)
MEMORY_CACHE_TTL=300000

# Persistent cache TTL in milliseconds (30 minutes)  
PERSISTENT_CACHE_TTL=1800000

# === Production Optimizations ===
# Enable gzip compression for API responses
ENABLE_COMPRESSION=true

# Enable ETag caching
ENABLE_ETAGS=true

# Maximum file size for uploads (50MB in bytes)
MAX_FILE_SIZE=52428800